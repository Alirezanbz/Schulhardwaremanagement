package com.example.schulhardwaremanagement.Repository;
import com.example.schulhardwaremanagement.Entity.Lagerort;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LagerortRepository extends JpaRepository<Lagerort, Integer> {
}
